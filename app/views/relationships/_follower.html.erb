<table class='table'>
  <tr>
    <th>follower</th>
    <th>
    <%= link_to user_followers_path(user) do %>
      <%= user.followers.count %>
    <% end %>
    </th>
  </tr>
  <tr>
    <th>follwered</th>
    <th>
    <%= link_to user_followings_path(user) do %>
      <%= user.followings.count %>
    <% end %>
    </th>
  </tr>
  <% if current_user.following?(user) %>
    <tr><td><%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, class: "btn btn-primary" %></td></tr>
  <% else %>
    <tr><td><%= link_to "フォロー", user_relationships_path(user.id), method: :post, class: "btn btn-success" %></td></tr>
  <% end %>
</table>